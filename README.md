# clic
# installation

You will need:

git

cmake

g++

Build Instructions:

(skip first step if given a tar.gz)

    git clone https://github.com/wetbadger/clic.git
    
    cd clic
    mkdir build
    cd build
    cmake ..
    cmake --build .
    make
    
run with:
    
    ./clic
    
